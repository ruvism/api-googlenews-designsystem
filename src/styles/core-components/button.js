import * as globalTokens from '../tokens/global';
import * as brandingTokens from '../tokens/branding';

const button = {
    fontSize: globalTokens.fontSizeSm,
    lineHeight: globalTokens.lineHeightTight,
    borderRadius: globalTokens.borderRadiusNone,
    padding: globalTokens.spacingSquishXs,
    fontFamily: globalTokens.fontFamilyHighlight,
    fontWeight: globalTokens.fontWeightMedium,
    color: brandingTokens.colorNeutral05,
    backgroundColor: brandingTokens.colorBrandPrimary03,
    backgroundHover: brandingTokens.colorBrandPrimary02,
};

export default button;