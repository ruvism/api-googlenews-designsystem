import * as globalTokens from '../tokens/global.js';
import * as brandingTokens from '../tokens/branding.js';

 const heading = {
    fontSize: globalTokens.fontSizeLg,
    lineHeight: globalTokens.lineHeightDistant,
    fontFamily: globalTokens.fontFamilyHighlight,
    fontWeight: globalTokens.fontWeightBold,
    color: brandingTokens.colorNeutral01,
    marginBottom: globalTokens.spacingStackXxxs,
};

export default heading;