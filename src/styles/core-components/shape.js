import * as globalTokens from '../tokens/global.js';
import * as brandingTokens from '../tokens/branding.js';

const shape = {
        borderRadius: globalTokens.borderRadiusNone,
        padding: globalTokens.spacingInsetLg,
        borderWidth: globalTokens.borderWidthThin,
        borderColor: brandingTokens.colorNeutral04,
        backgroundColor: brandingTokens.colorNeutral05
};

export default shape; 