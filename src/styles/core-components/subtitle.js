import * as globalTokens from '../tokens/global.js';
import * as brandingTokens from '../tokens/branding';

 const subtitle = {
    fontSize: globalTokens.fontSizeMd,
    lineHeight: globalTokens.lineHeightMedium,
    fontFamily: globalTokens.fontFamilyHighlight,
    fontWeight: globalTokens.fontWeightMedium,
    color: brandingTokens.colorNeutral02,
    marginBottom: globalTokens.spacingStackXxs,
};

export default subtitle; 